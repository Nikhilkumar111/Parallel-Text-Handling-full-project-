import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  // Add any Next.js specific config here if needed
  // No rewrites needed since API calls use NEXT_PUBLIC_API_BASE_URL directly
};

export default nextConfig;